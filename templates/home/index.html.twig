{% extends 'base.html.twig' %}

{% block title %}Inicio - BookMarket{% endblock %}

{% block body %}
    {# Página index explicando el proyecto, nivel DAW #}
    <h1>Bienvenido a BookMarket</h1>
    <p>
        BookMarket es una plataforma de compraventa de libros usados.
        Los usuarios pueden publicar libros, clasificarlos por categorías y gestionar compras de forma sencilla.
    </p>

    <h2 class="mt-4">Últimos libros publicados</h2>
    <ul>
        {% for book in books %}
            <li>{{ book.titulo }} - {{ book.autor }} ({{ book.categoria.nombre }})</li>
        {% else %}
            <li>No hay libros todavía.</li>
        {% endfor %}
    </ul>

    <h2 class="mt-4">Categorías disponibles</h2>
    <ul>
        {% for categoria in categorias %}
            <li>
                <a href="{{ path('app_categoria_show', {'id': categoria.id}) }}">{{ categoria.nombre }}</a>
                : {{ categoria.descripcion }}
            </li>
        {% else %}
            <li>No hay categorías.</li>
        {% endfor %}
    </ul>
{% endblock %}
