{% extends 'base.html.twig' %}

{% block title %}Registro{% endblock %}

{% block body %}
    <h1>Registro de usuario</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>Registrarse</button>
    {{ form_end(form) }}

    <a href="{{ path('app_login') }}">Volver al login</a>
{% endblock %}
