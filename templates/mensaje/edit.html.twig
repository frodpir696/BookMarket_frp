{% extends 'base.html.twig' %}

{% block title %}{{ mensaje.id ? 'Editar Mensaje' : 'Nuevo Mensaje' }}{% endblock %}

{% block body %}
    <h1>{{ mensaje.id ? 'Editar Mensaje' : 'Nuevo Mensaje' }}</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>{{ mensaje.id ? 'Actualizar' : 'Crear' }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_mensaje_index') }}">Volver a la lista</a>
{% endblock %}
