{% extends 'base.html.twig' %}

{% block title %}{{ book.id ? 'Editar Libro' : 'Nuevo Libro' }}{% endblock %}

{% block body %}
    <h1>{{ book.id ? 'Editar Libro' : 'Nuevo Libro' }}</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>{{ book.id ? 'Actualizar' : 'Crear' }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_book_index') }}">Volver a la lista</a>
{% endblock %}
